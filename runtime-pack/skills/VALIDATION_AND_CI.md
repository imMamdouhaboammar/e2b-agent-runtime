# Validation and CI Skill

Record validation evidence using `validation_record`. Run unit tests, linting, type-checking, and build validation. Validate that CI expectations are met before branch publication.

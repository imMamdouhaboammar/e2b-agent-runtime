# Context Checkpoints Skill

Save session checkpoints (`agent_create_checkpoint`) containing task scope, inspected paths, decisions, commit SHAs, and validation records to ensure session resume safety.

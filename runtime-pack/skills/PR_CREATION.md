# PR Creation Skill

Run publication preflight (`github_preflight_publish`), scan secrets, publish branch to GitHub (`github_publish_branch`), and generate PR handoff markdown (`github_prepare_pr_handoff`).

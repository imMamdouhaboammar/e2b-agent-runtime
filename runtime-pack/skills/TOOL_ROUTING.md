# Tool Routing Skill

Use `terminal_exec` for fast deterministic commands (builds, tests, status checks).
Use persistent PTY (`terminal_open`, `terminal_write`, `terminal_read`) for long-running processes, dev servers, or interactive tools.
Use `repository_read_file`, `repository_write_file`, `repository_apply_patch` for structured file manipulations inside `/workspace/repository`.

# Repository Discovery Skill

Discover package manager (npm, pnpm, yarn, uv, pip, cargo, go), lockfiles, manifests, governance documents (AGENTS.md, README.md), and candidate validation commands (`pnpm test`, `pnpm build`, etc.).

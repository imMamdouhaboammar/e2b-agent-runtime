# Security and Trust Skill

1. Never leak private keys, tokens, or credentials in commits, logs, or responses.
2. Reject path traversal out of `/workspace/repository`.
3. Disallow default branch push or force push.
4. Redact secrets automatically before returning command output.

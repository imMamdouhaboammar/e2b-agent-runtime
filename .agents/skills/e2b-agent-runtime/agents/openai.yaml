interface:
  display_name: "E2b Agent Runtime"
  short_description: "Repo-specific patterns and workflows for e2b-agent-runtime"
  default_prompt: "Use the e2b-agent-runtime repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true